concept_historical_artifacts
<- sc_node_class;  /* Определяет, что это класс объектов */
<- class;  /* Указывает, что это класс */
=> nrel_main_idtf:
    [Исторические артефакты]
    (* <- lang_ru;; *);
=> nrel_note:
    [Исторические артефакты — это объекты, созданные человеком, которые имеют культурное или историческое значение.]
    (* <- lang_ru;; *);

=> nrel_inclusion:
   concept_jewels_culture;;