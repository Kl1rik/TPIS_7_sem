concept_creation_date
<- sc_node_class;  /* Определяет, что это класс объектов */
<- class;  /* Указывает, что это класс */
=> nrel_main_idtf:
    [Дата создания]
    (* <- lang_ru;; *);
=> nrel_note:
    [Дата создания(добычи) драгоценности.]
    (* <- lang_ru;; *);;
